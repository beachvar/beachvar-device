services:
  device:
    build: .
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: unless-stopped
    # Use host network for network scanning to work properly
    network_mode: host
