# Gateway connection
GATEWAY_URL=wss://beachvar-gateway.vinicius.workers.dev
DEVICE_ID=your-device-uuid
DEVICE_TOKEN=your-device-token

# Optional: Path to store rotated tokens (local file)
TOKEN_FILE=.token
